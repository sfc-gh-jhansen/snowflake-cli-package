# Snowflake CLI Generic Package Plugin

A Snowflake CLI plugin that manages generic packages.

## Overview

Overview coming soon.

## Getting Started

### Prerequisites

- Clone to repository to your local dev environment
- Prereq 2
- Prereq 3

### Installation (for Development)

1. **Install Snowflake CLI**
   ```bash
   pip install snowflake-cli
   ```

2. **Install this plugin in editable mode**
   ```bash
   pip install -e .
   ```

3. **Enable this plugin**
   ```bash
   snow plugin enable package
   ```
   The `package` command should now be available in the CLI.

## Appendix

### Snowflake CLI Plugin Examples

Here are some examples of published Snowflake CLI plugins:
* Nextflow plugin by Haowei Yu
   * [Snowflake CLI Nextflow Plugin Repo](https://github.com/Snowflake-Labs/snowflake-cli-nextflow)
   * [Snowflake CLI Nextflow Plugin PyPI](https://pypi.org/project/snowflake-cli-nextflow/)
